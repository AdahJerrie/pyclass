# Day 6 will move into modules, imports, and organizing Python programs.
# We'll also start thinking about how real Python projects are structured, rather than keeping everything in one file.
# This is a transition from writing py code to organizing py code.
# In python you split code into modules, in Go it is packages.
# A module is essentially a way of packaging related Python code into one file.

import math

print(math.sqrt(25))
